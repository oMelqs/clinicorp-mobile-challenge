import {StyleSheet} from 'react-native'

const styles = StyleSheet.create({
  card: {
    borderRadius: 0,
    borderWidth: 0,
    backgroundColor: '#fff',
  },
  cardContent: {
    flexDirection: 'row',
    alignItems: 'center',
    paddingBottom: 16,
  },
  cardTexts: {
    marginRight: 40,
    paddingLeft: 8,
  },
  cardCover: {
    marginTop: -4,
    marginHorizontal: 24,
    borderRadius: 5,
  },
})

export default styles
